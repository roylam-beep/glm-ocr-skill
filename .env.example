# Z.ai API Key - 從 https://open.bigmodel.cn/ 獲取
ZAI_API_KEY=your_api_key_here

# GLM-OCR API 端點
ZAI_API_BASE=https://api.z.ai/api/paas/v4

# 預設掃描目錄（可留空，執行時指定）
DEFAULT_SCAN_DIR=./scans

# 預設輸出路徑（可留空，執行時指定）
DEFAULT_OUTPUT_DIR=./output
